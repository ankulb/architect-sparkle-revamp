## Update Hero first slide headline

Change the first slide in `src/components/home/Hero.tsx` (currently the Luxury Housing slide) so its headline matches the wireframe copy: **"Designing the future through Architecture, Interiors and Engineering"**.

### Change
In `SLIDES[0]`:
- `kicker`: keep as `"Luxury Housing"` — or drop it since the wireframe headline is brand-level, not category-level. **Question below.**
- `headline`: replace `"Homes shaped around the way people actually live"` with `"Designing the future through Architecture, Interiors and Engineering"`.

All other slides (Commercial, Data Centres, Hospitality, Interior Architecture) stay as-is. No changes to animation, layout, image, or timing.

### One clarification
The wireframe headline is brand-wide, but slide 1's image is Luxury Housing with kicker "Luxury Housing". Options:
1. Keep kicker "Luxury Housing" (small mismatch between kicker and headline).
2. Change kicker to something brand-level like "Team One Architects" or "Studio".
3. Hide the kicker on slide 1 only.

I'll default to **Option 1** (minimal change, only headline swapped) unless you say otherwise.
